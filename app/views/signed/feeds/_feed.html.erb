<div class="hero-unit" adjustTd=true>
   <div class="row-fluid">
      <div class="span14">
	      <div class="row-fluid">
		       <div class="span4">
	            <%= image_tag feed.user.profile_image_thumb %>
	         </div>
	         <div class="span17  span_content">
	            <%= render "feed_header", :feed => feed %>
	         </div>
	      </div><br>
      </div>
       <div class="span7">
	     <%= render "feed_controls", :feed => feed %>
	     </div>
   </div>
   <div class="row-fluid"><%= render "feed_content", :feed => feed %></div>
   <div class="row-fluid"><%= render "wow_pin_share",:feed => feed %></div> 
   <div class="row-fluid"><%= render "feed_comment", :feed => feed %></div>
   <div class="row-fluid"  id="heart_rating_<%= feed.id %>"><%= render "comment_emotions", :feed => feed %></div>
   <div class="row-fluid"><%= render "feed_count_commentslist", :feed => feed %></div>
</div>
<%= render "/signed/shared/emotions" %>
<div id="myCommentMoodModal-<%= feed.id %>" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style = "width :400px" feed-id="<%= feed.id %>">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true" id="cboxClose" ></button>
    <h6 id="myModalLabel">Add the option below that reflects your comment</h6>
  </div>
  <div class="modal-body">
<!--
    <% CommentMood.order_by("order ASC").each do |cm| %>
      <div class="span10">
        <div class= "row-fluid" >
          <% img = image_tag(cm.enabled_image, :title => cm.name)  %>
          <a href="#", onclick = "onComment($(this).attr('comment_mood_id'), $(this).attr('feed_id'));", feed_id = "<%= feed.id %>", comment_mood_id = "<%= cm.id %>"><% img + " " + "#{cm.name}" %></a>
        </div><br>
      </div>
    <% end %>
-->
    <% Comment_moods.keys.each do |no| %>
      <div class="span13">
        <div class= "row-fluid" >
          <% img = image_tag '/assets/comment_moods/'+Comment_moods[no] +'.png', :alt => "" %>
          <%#="/img/"+ Comment_moods[no] +".png" %>
          <a href="#", onclick = "onComment($(this).attr('comment_mood_id'), $(this).attr('feed_id'));", feed_id = "<%= feed.id %>", comment_mood_id = "<%= no %>"><%= img  %><%= " - " %><%= Comment_moods[no] %>   </a>
        </div><br>
      </div>
    <% end %>
  </div>
</div>

